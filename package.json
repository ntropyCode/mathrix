{
  "name": "mathrix",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check src/main.js && node --check src/worker.js && node --check src/arithmetic.js && node --check src/parser.js && node --check src/bit-field.js && node --test"
  },
  "engines": { "node": ">=22" }
}
